services:
  # 1. Frontend Service (Static Site)
  - type: web
    name: my-frontend
    runtime: static
    rootDir: apps/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    buildFilter:
      paths:
        - apps/frontend/**
      ignoredPaths:
        - apps/frontend/README.md

  # 2. Backend Service (Web Service)
  - type: web
    name: my-backend
    runtime: node
    rootDir: apps/backend
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: my-db
          property: connectionString
    buildFilter:
      paths:
        - apps/backend/**

# 3. Managed Database
databases:
  - name: my-db
    plan: free  # Use 'free' for testing or 'basic' for production
