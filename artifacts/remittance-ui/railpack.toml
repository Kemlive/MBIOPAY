[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @workspace/remittance-ui run build"

[deploy]
startCommand = "pnpm --filter @workspace/remittance-ui run serve"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/"
healthcheckTimeout = 30